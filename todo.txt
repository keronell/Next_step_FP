DEV-23 NEURAL MATCHER REWORK -- WHAT IS LEFT
Written 2026-07-31 after DEV-94 (commit 6f7ba12) on branch dev-23.
Updated 2026-08-03 after DEV-97, DEV-98 and DEV-99.

================================================================
WHAT IS ACTUALLY LEFT, AS OF DEV-99
================================================================
Execution-order items 1-11 are done AS FAR AS AN AGENT MAY TAKE THEM. Updated
2026-08-04: items (0) and (2) are now DONE. One thing remains, and it is not an
agent's:

  (0) DONE 2026-08-04, branch feat/adr-0005-ranking-only-percentages. NO JIRA
      TICKET YET -- DEV-101 is an unrelated chatbot ticket; do not cite it.
      ADR 0005's mitigation is now IMPLEMENTED. Was: documented in three places
      and built in none, so flipping the flag would have shipped the
      uncalibrated percentages the artifact's own caveat forbids.

      What landed: Matcher gained a `deployment` member parsed at load by the
      shared parse_deployment (an unrecognised status FAILS the load rather than
      defaulting permissive); _formula_scored was extracted from _match_formula
      so a career only the model ranked still gets a formula percentage; and for
      a ranking_only artifact matchPercent/score/score_breakdown are all the
      formula's while `reasons` stay model-derived.

      THE HUMAN CALL WAS MADE, AND IT AMENDED THE ADR. The non-monotonic-display
      question is answered by ordering the model's selected set by the formula's
      percentage, so the top row always carries the highest number. That means a
      ranking_only model supplies the SELECTION, not the ranking -- narrower than
      ADR 0005 as originally written. Recorded in the ADR and in CONTEXT.md's
      Ranking-Deployable entry. The 2 xfail(strict=True) tests XPASSed on landing
      and their markers were removed; the second is now asserted in its positive
      form (equality with the formula's percentage).

      MATCHER_MODEL_PATH IS STILL BLANK. This removes the incoherence blocking
      item (1); it does not approve it.

  (1) DEV-99 -- flip MATCHER_MODEL_PATH. Human approval only. Item (0) used to
      make this approval INCOHERENT -- "approve the flip" and "ship uncalibrated
      percentages" were the same action. AS OF 2026-08-04 THEY NO LONGER ARE, so
      the question is now the genuine one: is the evidence good enough? Inputs:
      docs/dev-23-nn-decision.md (evidence -- read section 7, Validity) and
      docs/dev-23-flip-readiness.md (mechanism). Production runs the formula.
      Jira status left at To Do; the finding was added as a comment.

  (2) DEV-89 -- q11-q18 reason rendering. DONE 2026-08-04, ON ITS OWN BRANCH OFF
      main (fix/dev-89-question-phrase-coverage, commit 4a5fc0d), Jira at Code
      Review. NOT on dev-23 and deliberately not merged into it -- it must reach
      main on its own. dev-23 picks it up when main merges.
      Measured against DEV-89's OWN acceptance denominator (the explainable
      universe: own-career fit/sem/skill PLUS all question features), renderable
      share went 0.572 -> 1.000. That is NOT comparable to the 0.293 DEV-99
      measured, which is the narrower question-only reading -- see the DEV-89
      reproduction record in data/scripts/README.md on main.
      Still a prerequisite of MERGING the NN serving work, now satisfied.

Plus one open trade reserved for a human: the IG step cap (section 2 below).

The three MECHANICAL DEV-99 criteria are verified and now tested (load-failure
fallback, version logging, rollback) -- see section 7 below. They are fine. The
switch is sound; it is wired to the wrong thing.

Section 0(a) below is STALE on this machine -- numpy is already installed in
backend/venv (2.5.1). Verify before acting on it elsewhere. Section 0(c) is
WRONG: e2e/ is untracked but NOT gitignored; .gitignore:29 is a DEV-93
checkpoint rule. It is still not ours -- never `git add -A`.

Baselines at DEV-99:
  services/matching        -> 146 passed + 2 xfailed  (was 138; the 2 xfails are
                              item (0) above and are expected to fail BY DESIGN)
  services/questionnaire   -> 19 passed (was 18)
  data/scripts/tests       -> 118 passed (training venv), 7 passed + 12 skipped
                              (backend/venv -- the 7 must not move). UNCHANGED.
  five service suites      -> 307 passed + 2 xfailed  (was 298; only matching and
                              questionnaire moved -- roadmap 30, auth 31,
                              history 81 are all unchanged)

NOTE: an earlier untracked todo.txt vanished during the DEV-96 session and was
never recovered. This file is written fresh from the plan's execution order and
the ticket state -- it is NOT a reconstruction of that one. It is committed this
time, so it survives the trip between machines.


================================================================
0. READ THIS FIRST ON THE OTHER PC -- IT WILL COST YOU TIME
================================================================

(a) backend/venv NEEDS NUMPY INSTALLED. It is a per-machine virtualenv, so the
    DEV-94 install did not travel with the commit. Until you run this, the
    137-test matching suite FAILS TO COLLECT (not fails -- fails to collect),
    because services/matching/app/services/matcher_nn.py imports numpy and the
    dispatch seam imports that module:

        backend/venv/Scripts/python -m pip install --no-deps "numpy>=1.24.0"

    Use --no-deps. numpy has been declared in backend/requirements.txt all
    along; the venv was simply out of sync with it. Do NOT install anything
    into data/venv-training -- that is the hash-pinned one the dataset digest
    depends on. See data/scripts/README.md, "Matcher training environment".

    Verify afterwards, in this order:
        cd services/matching && ../../backend/venv/Scripts/python -m pytest tests -q
            -> expect 137 passed
        backend/venv/Scripts/python -m pytest data/scripts/tests -q
            -> expect 7 passed, 9 skipped  (MUST NOT MOVE -- if numpy alone
               un-skipped a module, something is wrong)
        data/venv-training/Scripts/python -m pytest data/scripts/tests -q
            -> expect 83 passed

(b) THE FOUR SWEEP CHECKPOINTS DO NOT TRANSFER. They are gitignored and live in
    data/training/: round1_checkpoint.json, round2_checkpoint.json,
    round2_scoreboard_checkpoint.json, learning_curve_checkpoint.json. This is
    fine -- Rounds 1 and 2 and the learning curve are all DONE and their
    RESULTS are committed (round1_results.json, round2_results.json,
    learning_curve_results.json, plus the three reports). You only lose the
    ability to resume a killed sweep, and there is no sweep left to run.
    THERE IS NO ROUND 3. Do not re-run or re-tune any sweep.

(c) e2e/ is gitignored (.gitignore:29) and is not mine. Leave it alone.

(d) On Windows the repo docs' POSIX venv paths need Scripts/ where they write
    bin/.


================================================================
1. DONE: DEV-97 -- export the selected neural matcher
================================================================
LANDED 2026-08-03. data/scripts/export_nn_model.py writes
data/models/matcher_nn_v1.json. Record: data/scripts/README.md
"Reproduction record (DEV-97)".

  * Gate-1 revalidation reproduced the DEV-95 ship floor at max drift 0:
    stability 0.7345667591736047 CLEARS the hard floor, ECE
    0.13922660469462908 FAILS the mitigable one. The exporter refuses on
    stability and records-and-mitigates on ECE, per ADR 0005 and the
    ticket's own scope note. deployment.status is "ranking_only" and a
    caveat carries the mitigation to the response.
  * Deployment temperature 0.80, fitted on ensemble-averaged OOF. It
    SHARPENS, where the linear artifact's 1.05 softens.
  * PARITY CAUGHT A REAL DEFECT worth 1.03e-2 in probability. Five
    features are exactly constant, NNClassifier's scale_ = std + 1e-8
    does NOT make them standardize to zero (the float32 mean of 232 rows
    carries rounding, so training fed -0.9226 on game-dev_skill), and
    float64 serving computed -1.0149 instead. Fixed by folding the
    constant into the first layer's bias. Shipped divergence 3.15e-7.
  * The linear path had no sklearn-vs-served parity check. Added.

Superseded original notes below, kept for the reasoning:
================================================================
Execution-order item 9, second half. Writes data/scripts/export_nn_model.py,
mirroring export_model.py, plus Gate-1 revalidation of the exact exported
configuration and torch-vs-numpy parity tests.

DEV-94 hands it two HARD constraints. Getting either wrong ships an artifact
whose served explanation describes a different model:

  * The artifact must serialize ALL FIVE ensemble members. The selected config
    is SeedEnsemble(random_state=42, n_members=5, dropout=0.5,
    weight_decay=1e-2), hidden 64->32, members seeded random_state+i, rest at
    NNClassifier defaults. Read it from `selected_specification` in
    data/training/round2_results.json rather than retyping it.

  * IG EXPLAINS THE LOGIT OF THE MEAN PROBABILITY,
    g_c = log(mean_i softmax(z_i)_c) / T.  Therefore:
      - fit `temperature` on ENSEMBLE-AVERAGED OOF probabilities (that is
        export_model.py's deployment-temperature use of temperature_scale --
        ADR 0007 sanctions it for exactly this), and
      - revalidate Gate 1 on SeedEnsemble.predict_proba, i.e. the mean of the
        members' probabilities.
    Do NOT revalidate mean-of-members'-logits. mean(softmax(z)) is not
    softmax(mean(z)), and the whole DEV-94 attribution rests on which one.
    ADR 0007's per-outer-fold cross-fitting still governs any REPORTED ECE;
    the shipped constant is the pooled deployment one. Two different fits.

The artifact schema matcher_nn.py already reads and validates (see
services/matching/tests/conftest.py::nn_artifact for a worked example):
    model_type: "probability_averaged_mlp_ensemble"
    feature_version, feature_names, careers
    scaler_mean, scaler_scale        (ONE shared scaler -- every member
                                      standardizes on the same training stats)
    activation: "relu"               (anything else is refused at load)
    members: [ {"layers": [{"weight": [[..]], "bias": [..]}, ...]}, ... ]
                                      weight is (out_features, in_features),
                                      same convention as the linear artifact's
                                      `coef`
    temperature, label_source, caveats
Depth, widths and member count are all read from the artifact -- nothing is
hardcoded, so the exporter is free to change the shape.

Also owed by DEV-97: the torch-vs-numpy parity tests plan Step 5.2 asks for.
They were deliberately NOT done in DEV-94 -- backend/venv has neither torch nor
a trained artifact, so they belong in the training venv alongside the exporter.


================================================================
2. THE OPEN DECISION I COULD NOT MAKE -- NEEDS A HUMAN
================================================================
THE IG STEP CAP IS SHORT -- BUT BY LESS THAN DEV-94 COULD TELL.

MEASURED ON THE REAL ARTIFACT (DEV-97, 2026-08-03), which is what section
2 below asked for and could not do:

    cd services/matching && ../../backend/venv/Scripts/python         tests/ig_diagnostics.py --artifact ../../data/models/matcher_nn_v1.json

    640 explanations : 75 fell through = 11.7%, strict median 5.17e-4
    1600 explanations: 167 fell through = 10.4%, strict median 5.13e-4

DEV-94's synthetic 60.9% was pessimistic by roughly a factor of five, and
its prediction that a weight_decay=1e-2 network would behave better was
right. The strict median now sits UNDER the 1e-3 tolerance rather than
above it. "A large minority of careers" is no longer the right
description of the shipped model; "about one in nine" is.

THE DECISION IS STILL A HUMAN'S -- it is resized, not settled. The three
options below are unchanged as options.

The integrand jumps at every ReLU breakpoint, so the midpoint Riemann sum is
O(1/m), not O(1/m^2). At the plan's tolerance (relative completeness residual
< 1e-3, measured against the centered delta being explained) and its cap of 512
steps, a large minority of careers emit NO model-derived reasons and fall
through to the non-model wording -- 60.9% on the measured fixture, 33.9% when
the same fixture's random weights are scaled by 0.3. The DIRECTION is robust,
the PERCENTAGE is not; quote the direction.

Beating O(1/m) requires locating the breakpoints, which is the analytic
activation-pattern tracking plan Step 5.2 explicitly rejects. So this is a real
accuracy / latency / tolerance trade, not a bug. The options are:
    - raise the cap (costs latency: ~200 ms/request today at TOP_N=3 and m=512;
      roughly linear in m),
    - loosen the tolerance, or change "relative" to mean relative to the
      attribution mass rather than the delta (Attribution.attribution_mass is
      carried so this is one line to evaluate -- at that reading essentially
      nothing falls through at either weight scale), or
    - accept the fall-through as designed ("better silence than attributions
      that do not sum to what they claim").

(DONE: measured above. ig_diagnostics.py now takes --artifact; with no
--artifact it still reproduces the DEV-94 synthetic table exactly, which is
what proves the change measures the artifact rather than moving goalposts.)

Full write-up: docs/dev-23-nn-rework-plan.md Step 5.2, and the DEV-94
reproduction record in data/scripts/README.md.


================================================================
3. THE MERGE BLOCKER: DEV-89 -- q11-q18 attribution  [DONE 2026-08-04]
================================================================
DONE on fix/dev-89-question-phrase-coverage (commit 4a5fc0d, off main), Jira at
Code Review, awaiting merge to main. Renderable share of the AC's explainable
universe went 0.572 -> 1.000; do NOT quote that against DEV-99's 0.293, which
measures the narrower question-only denominator. Everything below is the
ORIGINAL sizing, kept because it is why the ticket existed.

Own ticket, OWN BRANCH OFF main, own schedule -- it is a
prerequisite of MERGING the NN serving work, not of building it, and it
degraded production output on the currently-served path.

reason_builder.py:18 defines QUESTION_PHRASES for q1-q10 only AND that dict is
also the iteration set (reason_builder.py:71 -- the plan said :72, corrected in
DEV-94). The bank is 18 questions, so 16 of the 36 question features -- 44% of
the question-feature attribution surface -- are silently discarded. DEV-94's
integrated gradients compute them correctly and they are thrown away until this
lands.

Three tests the plan specifies (Step 5.1): coverage of every id in
questions.json; attribution mass renderable >= 0.99 against the explainable
universe; and a wording-drift snapshot pairing each question's text with its
phrase.

MEASURED ON THE REAL ARTIFACT (DEV-99, 2026-08-03). The "44%" above is a
FEATURE COUNT and it understates the damage badly. By ATTRIBUTION MASS on
matcher_nn_v1, over 300 explanations (positive mass only -- negative
attribution is unrenderable for every question, so counting it would inflate
the gap):

    renderable share, mean   0.293   (median 0.158)
    DISCARDED share, mean    0.707
    explanations losing the MAJORITY of their question mass  248/300 = 82.7%

So ~71% of the question attribution the model actually produces never reaches
a sentence. That is the quantitative form of DEV-98's qualitative prediction:
q11-q18 carry zero questionnaire weight and signal only through per-option
bonuses, so they discriminate, and a learned model leans on them hardest.

    cd services/matching && ../../backend/venv/Scripts/python \
        tests/flip_diagnostics.py

The plan's ">= 0.99 renderable" acceptance test would currently measure 0.293.
Nothing here touches reason_builder.py -- DEV-89 is still its own ticket on its
own branch off main.


================================================================
4. DONE: DEV-98 -- the decision document
================================================================
LANDED 2026-08-03. docs/dev-23-nn-decision.md, plus the plan's Step 4.1 / Step 6
writeups and the CONTEXT.md vocabulary fix. Record: data/scripts/README.md
"Reproduction record (DEV-98)". Nothing was retrained, re-exported or re-swept;
the digest is untouched and no served output moved.

  * THE VOCABULARY GAP IS CLOSED BY NAMING THE STATE, not by stretching
    "Deployable". CONTEXT.md gains RANKING-DEPLOYABLE: Servable, revalidated,
    clears the HARD half of the ship floor and fails the MITIGABLE one. Its
    machine-readable form is the artifact's deployment.status "ranking_only",
    so the word and the field are one concept. It is explicitly NOT a weaker
    Deployable and can never make a model the Incumbent.
    The only deployability sentence asserted anywhere: matcher_nn_v1 is
    Ranking-Deployable -- it may serve the RANKING under ADR 0005's mitigation,
    with displayed percentages falling back to the formula's.
  * THE ~94% CIRCULARITY CLAIM WAS A LITERAL. Nothing computed it, and it sits
    inside both artifacts' caveats where it reaches users. Now computed by
    data/scripts/measure_circularity.py: 615/651 = 94.5% (94.9% at label level,
    79.3% under the strict +3-rule reading, and 678/678 = 100% shortlist
    containment which is STRUCTURAL, not evidence). The claim reproduces.
    tests/test_measure_circularity.py pins the shipped caveat text against the
    measurement, so regenerating the labels breaks the build.
  * TWO RECORDED FIGURES WERE WRONG. Fleiss kappa is 0.857 for the shipped
    panel-v2.1.0 labels, not the plan's "0.88-0.92" (which matches no run --
    0.930 was the rejected clone-persona v1.0.1, 0.864 was v1.1.0). And ADR
    0002's "logistic_tuned clears both (ECE 0.0341)" is gate1_verdict.json's
    `logistic` row, the fixed C=1.0 config the artifact serialises; Gate 2's
    logistic_tuned selects C per fold and its RAW ECE is 0.103, which would fail
    the same floor. Both corrected in place (ADR 0005 gained an "Observed on..."
    note; the decision itself is unchanged).
  * THE INCUMBENT IS THE ARTIFACT, NOT logistic_tuned. CONTEXT.md defines
    Incumbent against Deployable, and logistic_tuned has no single configuration
    and no artifact, so it cannot be Deployable. The Incumbent is
    matcher_logistic_v2.json (fixed C=1.0). CONSEQUENCE, and it is a real
    limitation: every effect size in this deliverable is against logistic_tuned,
    which is NOT the model that would actually be replaced, and no round measured
    the NN against the artifact's configuration. Unpaired evidence says that gap
    is WIDER (C=1.0 scores top-2 0.8707 vs the nested 0.849), so it is stated as
    a direction and NO delta is computed from it. Both code-review axes caught
    the original conflation independently.
  * The honest-framing paragraph is section "The framing" at the top of the
    document, before any number: these metrics can rank learned models against
    each other, they CANNOT justify replacing the formula, and the NN ships
    because the project requires it (ADR 0004).

Execution-order item 11 / DEV-99: report and WAIT on the MATCHER_MODEL_PATH
question. Nobody is authorised to flip it -- not .env.example, not backend/.env,
not docker-compose.yml. It is blank today, so production runs the formula.
The decision document is the input to that approval, not the approval.
DONE as far as an agent may take it -- see section 7 below.


================================================================
7. DONE: DEV-99 -- flip readiness (NOT the flip)
================================================================
LANDED 2026-08-03. docs/dev-23-flip-readiness.md,
services/matching/tests/test_flip_readiness.py (10 tests: 8 pass, 2 xfail),
services/matching/tests/flip_diagnostics.py, one new questionnaire test, an
"Observed on..." note in ADR 0005, a pointer at the end of the decision
document, and in-place wording fixes to CLAUDE.md and README.md. Record:
data/scripts/README.md "Reproduction record (DEV-99)". MATCHER_MODEL_PATH IS
UNTOUCHED, Jira status left at To Do, finding added as a Jira comment.

  * THE HEADLINE IS THE GAP, not the verification. See item (0) at the top of
    this file. ADR 0005's mitigation exists in the artifact, the ADR and
    CONTEXT.md and NOWHERE IN CODE. That makes the approval DEV-99 asks for
    incoherent until it is settled, because the flip would ship exactly the
    uncalibrated percentages the artifact's caveat forbids.
  * SIZED: same request, formula displays top-1 mean 73.3% and matcher_nn_v1
    displays 58.0%. matcher_logistic_v2 displays 76.4%. (Fixture RAG signals --
    chromadb is absent from backend/venv -- so this sizes the two OUTPUT
    SCALES, which is a property of the scorers. Ranking-agreement rates between
    scorers ARE fixture-dependent and are deliberately NOT quoted anywhere.)
  * SECOND SURFACE, easy to miss: matchPercent renders in Results.jsx:96 AND
    History.jsx:187; model_caveats renders in Results.jsx:43 ONLY. A persisted
    uncalibrated percentage is re-shown later with no caveat beside it.
  * THE THREE MECHANICAL CRITERIA ARE FINE and are now tested by RUNNING the
    real lifespan, not by reading source: all four load-failure modes (stale
    v1, missing file, unknown model_type, feature-version mismatch) fall back
    to output IDENTICAL to the no-model response; startup logs the loaded
    version for both artifacts; rollback works in-process with no module
    reload.
  * ROLLBACK LIMIT, stated because it is not restored: model_version and
    model_caveats are embedded per rec and persisted verbatim
    (SubmissionHistoryItem.recommendations: list[dict]). Clearing the flag
    changes NEW submissions only and rewrites nothing already stored. Rollback
    is complete forward, not backward.
  * ONE REPO-WIDE WORDING CORRECTION. Several docs say backend/.env's
    MATCHER_MODEL_PATH "never reaches the services". `docker compose config`
    says it reaches auth AND roadmap via env_file -- both ignore it, since only
    services/matching/app/main.py:44 reads matcher_model_path even though
    common/config.py:22 defines it for every service. For matching, compose's
    `environment:` key overrides `env_file:` and resolves to ''. The
    CONCLUSION is right (it never reaches the service that USES it); the
    literal sentence is not.
  * DEV-89 sized on the real artifact -- see section 3.

DEV-100 (separate, unrelated to the above): two_tower seeds from PROCESS-GLOBAL
torch RNG rather than per fit, so its predictions depend on how many torch fits
preceded it. If anything ever touches train_models.main(), keep every
torch-fitting model AFTER two_tower or its recorded numbers move.


================================================================
5. STOP CONDITIONS -- NOTHING BELOW MAY MOVE
================================================================
dataset_digest
  2bdd5ec99d6a49a2a19c40163cf7a69d560453e3095bc6b4241c6065f18a4b27

Gate 1 (data/training/gate1_verdict.json), ECE / top-2 stability:
  logistic  0.034099440082920096 / 0.637516702641587
  lightgbm  0.128155228434309    / 0.5566450817144618
  small_nn  0.06183095636038942  / 0.6153150375167026
  small_nn reseeded stability 0.6670161373214101

Gate 2, top-2 / cross-fitted ECE:
  gbt_tuned 0.892 / 0.040 . logistic_tuned 0.849 / 0.061
  small_nn  0.845 / 0.102 . two_tower 0.763 / 0.081
  residual_matcher 0.849 / 0.061

Round 1 (nn_rework.md), Round 2 (nn_rework_round2.md) and the learning curve
(learning_curve.md) are recorded MEASUREMENTS. So is the currently served
output: the service suites are the evidence, and within them the matching
suite is what shows the formula path is inert. (Earlier revisions of this
file quoted 297 and 108 here; both were stale even when written. Use the
counts in section 6, which are measured at each commit.)


================================================================
6. STATE AS OF THIS COMMIT
================================================================
Branch dev-23. Done: DEV-87, 88, 90, 91, 92, 93, 95, 96, 94, 97, 98, and 99 as
far as an agent may take it (the FLIP itself is not done and is not an agent's).
Service suites 307 passed + 2 xfailed = questionnaire 19 + matching 146+2 +
roadmap 30 + auth 31 + history 81. Only matching (was 138) and questionnaire
(was 18) moved; DEV-99 adds no production code and touches no served output --
the 138 pre-existing matching tests all still pass. data/scripts/tests UNCHANGED
at 118 (training venv) and 7 passed + 12 skipped (backend/venv -- the 7 did not
move, so nothing un-skipped and the venv separation is intact).

CONTEXT.md now names FIVE states. The first four are evaluated in order and none
implies an earlier one; DEPLOYABLE and RANKING-DEPLOYABLE are the two TERMINAL
states and are mutually exclusive.

matcher_nn_v1 is SELECTED, SERVABLE and RANKING-DEPLOYABLE. It is NOT Qualified
(stable, not calibrated) and therefore NOT Deployable, and it cannot become the
Incumbent, which is defined against Deployable. THE INCUMBENT IS THE ARTIFACT
matcher_logistic_v2.json (fixed C=1.0) -- NOT logistic_tuned, which is a nested
protocol re-selecting C per outer fold, has no single configuration and no
artifact, and therefore cannot be Deployable. (An earlier revision of this file
said "the Incumbent is logistic_tuned, as exported to matcher_logistic_v2.json",
which is the exact conflation DEV-98 was written to correct; fixed in DEV-99.)
NEITHER IS SERVED: MATCHER_MODEL_PATH is blank and production runs the formula.

DEV-99 CAVEAT ON "RANKING-DEPLOYABLE" -- RESOLVED 2026-08-04. That
state permitted serving the ranking with displayed percentages falling back to
the formula's, and the serving path could not do it. It can now. The resolution
AMENDED the state rather than just implementing it: a ranking_only model supplies
the SELECTION and the formula ranks within it, so the displayed percentages are
monotonic. CONTEXT.md's entry and ADR 0005 both say so.

MATCHER_MODEL_PATH is unchanged: blank in .env.example, blank by default in
docker-compose.yml, blank in the repo-root .env that compose interpolates, and
backend/.env:23 still points at the stale matcher_logistic_v1.json. That value
reaches auth and roadmap via env_file and is ignored by both (only
services/matching/app/main.py:44 reads the setting); for matching, compose's
`environment:` key overrides `env_file:` and resolves to ''. So it never reaches
the service that USES it -- and the artifact is refused on load anyway. Earlier
notes said "never reaches the services", which is the right conclusion from a
false premise. Production runs the formula. Flipping it is DEV-99 and needs
human approval.
