# Models Directory

This directory contains model files for downstream tasks such as:

- User classification models
- Label prediction models
- Multi-label classification models
- Embedding models

## Future Structure

```
models/
├── user_classifier.py        # User classification model
├── label_predictor.py        # Label prediction model
├── embeddings.py             # Embedding models
└── trained_models/           # Saved model artifacts
    ├── user_classifier.pkl
    └── label_predictor.pkl
```

