node_modules/
backend/node_modules/
backend/venv/
# Isolated training stack (data/requirements-training.txt) — kept out of
# backend/venv so service-test installs can never move numpy under the dataset digest.
data/venv-training/
backend/__pycache__/
backend/**/__pycache__/
*.pyc
*.pyo
*.pyd
.Python
frontend/node_modules/
backend/data/*.db
*.db
*.log
.env
.DS_Store
dist/
build/


# ChromaDB generated vector store
data/jobs/chroma/
.playwright-mcp/

# Local scratch / PR-review temp (zips, extracted PRs, pytest temp dirs)
.tmp/

# Resume artifact for the DEV-93 Variant sweep. Holds per-profile indicator arrays
# for every (seed, model) so a killed run resumes instead of restarting — an
# intermediate, not a deliverable. The deliverables are round1_results.json and
# nn_rework.md, which are tracked.
data/training/round1_checkpoint.json
